"""Define useful constants for the entirety of the integrations project.
"""

import os

CREATE = "create"
LIST = "list"
DELETE = "delete"
UPDATE = "update"

ROOT_DIR = os.path.dirname(os.path.realpath(__file__))
